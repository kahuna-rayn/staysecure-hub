// Organisation module exports - Core business logic only
// UI components should be provided by the consuming application

// Types only for now - no UI components
export type {
  Organisation,
  User,
  Department,
  Location,
  Role,
  Certificate,
  OrganisationContextValue
} from './types';